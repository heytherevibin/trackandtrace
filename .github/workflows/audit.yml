# Production dependencies only; fails on high or critical advisories.
# Runs weekly and when the dependency files change, so a new advisory
# never blocks unrelated work.
name: Dependency audit

on:
  pull_request:
    branches: [main]
    paths: [package.json, package-lock.json]
  push:
    branches: [main]
    paths: [package.json, package-lock.json]
  schedule:
    - cron: "30 3 * * 1" # Mondays, 09:00 IST
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
      # npm audit reads package-lock.json, so no install is needed.
      - run: npm audit --omit=dev --audit-level=high
